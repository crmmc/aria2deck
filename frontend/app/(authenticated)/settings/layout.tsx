import type { Metadata } from "next";

export const metadata: Metadata = { title: "系统设置 - aria2 控制器" };

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
